# CDP-3C · Paket v6 → v7 değişiklik tablosu (8 kapanış)

Her madde: **eski (v6) → yeni (v7) → etkilenen dosya(lar)**. Production'a uygulanmadı, CI koşulmadı, kill-switch değişmedi. Değişiklikler gömülü Postgres 16.2'de çalıştırılıp doğrulandı; ham kanıt `gates/LOCAL_GATE_EVIDENCE.log`.

| # | Konu | Eski (v6) | Yeni (v7) | Dosya |
|---|---|---|---|---|
| 1 | Preflight obje listesi eksik/drift | elle 14 tablo/5 type/4 func | **KANONİK manifest** (`gen_object_manifest.sh` → `cdp3c_objects.manifest`: 20 tablo+12 type+48 func); `cdp3c_preflight_objects.sh` (salt-okunur) pre-apply herhangi biri varsa fail-closed; `cdp3c_manifest_check.sh` up.sql↔manifest drift gate (20/12/48 sağlaması); preflight.sql'den elle liste kaldırıldı; negatif fixture'lar (eksik tablo+type+func) | `gates/gen_object_manifest.sh`, `gates/cdp3c_objects.manifest`, `gates/cdp3c_preflight_objects.sh`, `gates/cdp3c_manifest_check.sh`, `gates/cdp3c_preflight.sql`, `gates/cdp3c_preflight_selftest.sh` |
| 2 | Selftest destructive, local guard yok | `drop schema` guard'sız | **İki-katman + sentinel** `_ephemeral_guard.sh`: opt-in `CDP3C_ALLOW_DESTRUCTIVE=1` + host yalnız loopback/localhost/unix-socket + baseline'ın yarattığı `_cdp3c_ephemeral_ok` sentinel; DROP/ALTER/INSERT ÖNCESİ fail-closed; aynı guard `cdp3c_concurrency.sh`'e de uygulandı; `cdp3c_guard_selftest.sh` (non-local/sentinel-yok/opt-in-yok negatifleri + pozitif) | `gates/_ephemeral_guard.sh` (yeni), `gates/cdp3c_preflight_selftest.sh`, `gates/cdp3c_concurrency.sh`, `gates/cdp3c_baseline.sql` (sentinel), `gates/cdp3c_guard_selftest.sh` (yeni) |
| 3 | Negatif selftest workflow'da yok | yalnız yerel | Workflow'a **ayrı isimli gate'ler**: `manifest_check`, `guard_selftest`, `preflight_objects`, `privilege_matrix`, `preflight_selftest` (AYRI ephemeral DB `cdp3c_selftest`'te; ana DB'yi bozmaz); her biri sentinel + `GATE_FAILED:<ad>` + log artifact | `.github/workflows/cdp3c-gates.yml` |
| 4 | Bazı gate'ler GATE_FAILED üretmeden çıkabilir | `set -euo pipefail; out="$(psql…)"` → set-e command-sub'da erken çıkış | Sözleşme: `set -uo pipefail` (−e YOK) + `out="$(…)"; rc=$?` + log + `if rc≠0 \|\| !grep PASS → GATE_FAILED:<ad>; exit 1`; preflight/invariants/tests/concurrency/downsoft/objects/privilege hepsi bu desende; negatif fixture (rc=2 → GATE_FAILED) evidence'ta | `.github/workflows/cdp3c-gates.yml`, `gates/LOCAL_GATE_EVIDENCE.log` |
| 5 | approve 0C replay hash'e bağlı | fingerprint current hash içeriyordu → içerik değişince eski idem retry conflict | Fingerprint **IMMUTABLE request-intent** (actor\|text_version_id\|evidence\|reason; hash YOK); idem-first; approval hâlâ **kilitli current hash**'e bağlanır; replay eski sonucu döner, yeni hash için YENİ idem; T39 (same-hash replay / edit-eski-idem / edit-yeni-idem) | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T39) |
| 6 | legal_notice race kanıtı raporla uyuşmuyor | S3 yalnız anon_consent↔merge | Ayrı **S5 legal_notice_record↔anon_merge** deterministik senaryo: notice kazanır→tam 1 event+merge tamamlanır (idem replay aynı sonuç); merge kazanır→notice yalnız `notice_anon_subject_invalid`; generic/permission/boş → kırmızı | `gates/cdp3c_concurrency.sh` |
| 7 | Privilege matrisi "tüm helper" değil | seçili liste; trigger fn'leri yok | **DİNAMİK** `cdp3c_privilege_matrix.sh`: public şemasındaki TÜM fonksiyonlar taranır — anon hiçbirini, authenticated yalnız 3'ü; beklenmeyen authenticated-executable → FAIL; service_role manifest RPC'leri; baseline `_admin_active/_has_role` de authenticated/anon'a kapatıldı | `gates/cdp3c_privilege_matrix.sh` (yeni), `gates/cdp3c_baseline.sql` |
| 8 | Rapor/manifest eski bilgi | sentinel-teardown öncesi sıra + T01–T33 | Gerçek sıra: gates→teardown→final secret-scan→**sentinel(if:success)**→artifact; T01–**T39**; gate listesi güncel; yerel preflight EXPECTED-FAIL ayrımı korundu | `CDP3C_PACKAGE_REPORT.md`, `CDP3C_DELIVERY_MANIFEST.md`, `CDP3C_TEST_MATRIX.md` |

## Yerel kanıt özeti (BÖLÜM A/B/C — CI DEĞİL, production DEĞİL)
`gates/LOCAL_GATE_EVIDENCE.log`: manifest_check(20/12/48) · guard_selftest · secret_scan self-test · baseline · preflight_objects(PRE-APPLY, 80 nesne yok) · up + reapply · invariants(`CDP3C_INVARIANTS_PASS`) · tests(`CDP3C_TESTS_PASS`, T01–T39) · privilege_matrix(dinamik) · concurrency(5 senaryo) · down_soft + downsoft_assert · secret_scan(paket) · preflight_selftest(6 contract + 3 obje negatif + pozitif) → hepsi exit=0. #4 negatif fixture rc≠0→GATE_FAILED. **Yerel preflight contract EXPECTED-FAIL** (shim'de gerçek Vault yok; CI'da gerçek Vault ile PASS). GitHub ephemeral CI + production preflight: ÇALIŞTIRILMADI.

## Kill-switch
admin-delete-user kill-switch v3'e dokunulmadı.
