# CDP-3C · Paket v4 → v5 değişiklik tablosu (13 kapanış bulgusu)

Her madde: **eski (v4) → yeni (v5) → etkilenen dosya(lar)**. Production'a uygulanmadı, CI koşulmadı, kill-switch değişmedi. Tüm değişiklikler gömülü **Postgres 16.2** üzerinde (yerel) çalıştırılıp doğrulandı; kanıt: `gates/LOCAL_GATE_EVIDENCE.log`.

| # | Konu | Eski (v4) | Yeni (v5) | Dosya |
|---|---|---|---|---|
| 1 | 3 gate fail-closed değil | baseline/up/reapply `set -euo pipefail` yoktu; `psql|tee||fail` tee'nin 0'ıyla maskelenebilirdi | Üçü de `set -euo pipefail` + blok form `\|\| { echo GATE_FAILED; exit 1; }`; negatif fixture (hatalı SQL) evidence'ta **exit=1** ile kanıtlandı | `.github/workflows/cdp3c-gates.yml`, `gates/LOCAL_GATE_EVIDENCE.log` |
| 2 | Sentinel erken yazılıyordu | `LOCAL_CDP3C_GATES_PASS` teardown/scan'den önceydi | Sıra: gates → teardown(`always`,residue=0) → final secret-scan(`always`) → **sentinel (if:success)** → tek artifact(`always`); önceki hata sentinel'i SKIP eder | `.github/workflows/cdp3c-gates.yml` |
| 3 | down_soft eski imza + eksik | `legal_notice_record` eski imzayı düşürüyordu; `admin_w_approve_consent_text` hiç düşmüyordu | Güncel `legal_notice_record(legal_subject_kind,uuid,…)` + `admin_w_approve_consent_text(...)` eklendi; `downsoft_assert` artık **21 yazım RPC'sini allowlist** ile yoklar | `CDP3C_down_soft.sql`, `gates/cdp3c_downsoft_assert.sql` |
| 4 | anon consent↔merge race | consent_set `subject+purpose`, merge `anon_merge+subject` — farklı lock | İkisi de **`anon_subject`|subject** lock'u; consent_set lock **sonrası** expiry/merged re-read; gerçek iki-bağlantı testi: merge sonrası event YOK, subject merged (concurrency S3) | `CDP3C_up.sql`, `gates/cdp3c_concurrency.sh` |
| 5 | pointer invariant tek yönlü | yalnız pointer değişince kontrol; doğrudan controller deaktivasyonu kaçıyordu | **DEFERRABLE** constraint trigger (`_authoritative_consistency`): pointer↔tek-aktif-controller çift yönlü + aktif metin yalnız pointer'a bağlı; controller switch orphan metinleri deaktive eder; `_consent_text_biu` authoritative kontrol; doğrudan service_role deaktivasyon negatifi (T34, SET CONSTRAINTS IMMEDIATE) | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T34) |
| 6 | text switch deadlock riski | önce `FOR UPDATE` sonra advisory lock → deadlock | Tüm text-switch **tek global advisory lock** ile serialize; iki-farklı-hedef paralel aktivasyon testi: deadlock yok, tam 1 aktif (concurrency S4) | `CDP3C_up.sql`, `gates/cdp3c_concurrency.sh` |
| 7 | 0C fingerprint eksik | approve fp'de `reason` yoktu; `anon_subject_create` idem'siz | approve fp'ye reason eklendi (aynı idem farklı reason→conflict); `anon_subject_create(request_id,idem)` 0C'ye geçti (retry mükerrer subject üretmez) | `CDP3C_up.sql` |
| 8 | legal_notice subject/source matris | yalnız uuid null kontrolü | Kapalı subject_kind×source matrisi + varlık: member_uid→gerçek member+{signup,pref_center,system}; anon_subject→var+süresi geçmemiş+merge olmamış+{cookie_banner,system}; negatifler T35 | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T35) |
| 9 | suppression çapraz kombinasyon | bağımsız IN listeleri → push+all_email vb. geçiyordu | **Exact (channel,scope) çiftleri** allowlist + reason bu çiftler üzerinde; çapraz-negatifler T36 (push+all_email, email+global, global+all_email reddi) | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T36) |
| 10 | preflight iddiası koddan geniş | email nullability değerlendirilmiyor, admin_write_log tipleri/pgcrypto şema/extensions fn imzaları/vault yüzeyi doğrulanmıyordu; mevcut obje yalnız warning | Email nullability "fail-closed supported state" (validated değil) olarak işaretlenir; admin_write_log tip kontratı; pgcrypto **extensions şemasında** + digest/hmac/gen_random_bytes imzaları; vault.secrets/decrypted_secrets/create_secret; mevcut CDP-3C nesnesi → **fail-closed** | `gates/cdp3c_preflight.sql` |
| 11 | concurrency same-payload eksik assertion | yalnız audit=1; duplicate-key başarısı PASS sayılabilirdi | Same-payload: iki exit 0 + aynı JSON + audit=1 + write_ops=1 + state=1; diff-payload: tam 1 başarı + **idempotency_conflict** (duplicate-key = KUSUR→kırmızı) | `gates/cdp3c_concurrency.sh` |
| 12 | test/invariant kapsamı | `consent_text_approvals` invariants allowlist'te yoktu; matris eski T numaraları | approvals allowlist'e eklendi; sentinel gerçek isimli T01–T36 listesi; matris v5 notu | `gates/cdp3c_invariants.sql`, `CDP3C_TEST_MATRIX.md` |
| 13 | yerel koşum kanıtı yok | yalnız anlatım | `gates/LOCAL_GATE_EVIDENCE.log`: PG sürümü + tam komutlar + her gate exit code + T01–T36 isimli liste + concurrency + downsoft + secret-scan + negatif-fixture(exit=1); PII/secret temiz; SHA256SUMS kapsamında | `gates/LOCAL_GATE_EVIDENCE.log`, `SHA256SUMS` |

## Yerel kanıt özeti (CI DEĞİL, production DEĞİL)
`gates/LOCAL_GATE_EVIDENCE.log` — PostgreSQL 16.2: baseline OK · up OK · up(idempotent) OK · invariants `CDP3C_INVARIANTS_PASS` · tests `CDP3C_TESTS_PASS` (T01–T36) · concurrency `CDP3C_CONCURRENCY_PASS` (4 senaryo: same-payload, diff-payload-conflict, anon-race, text-switch) · down_soft OK · downsoft_assert `CDP3C_DOWNSOFT_ASSERT_PASS` · secret_scan self-test + paket temiz · negatif fixture exit=1 (fail-closed kanıtı). preflight yerelde extension adımına kadar geçer (gerçek pgcrypto/vault prod/CI'da).

## Kill-switch
admin-delete-user kill-switch v3'e dokunulmadı.
