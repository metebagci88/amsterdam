# CDP-3C · Paket v7 → v8 değişiklik tablosu (8 kapanış)

Her madde: **eski (v7) → yeni (v8) → etkilenen dosya(lar)**. Production'a uygulanmadı, CI koşulmadı, kill-switch değişmedi. Gömülü Postgres 16.2'de çalıştırılıp doğrulandı; ham kanıt `gates/LOCAL_GATE_EVIDENCE.log`.

| # | Konu | Eski (v7) | Yeni (v8) | Dosya |
|---|---|---|---|---|
| 1 | Ayrı DB create transaction-block'ta düşer | `psql -c "drop database…; create database…"` (CREATE DATABASE cannot run inside a transaction block) | DROP ve CREATE **ayrı `psql -c`**, her biri `rc=${PIPESTATUS[0]}` + `GATE_FAILED:selftest_db_drop`/`_create`; ayrıca selftest DB'ye baseline (`_baseline`); cleanup DROP maskelenmez → `GATE_FAILED:selftest_db_cleanup` | `.github/workflows/cdp3c-gates.yml` |
| 2 | preflight_objects DB hatasında fail-open | `Q(){ psql … 2>/dev/null }` → hata boş dönerdi, yanlış PASS | Her sorgu `2>&1` + exit-code kontrolü → tek hata bile `GATE_FAILED:preflight_objects_query`; manifest okunamaz/`bad_manifest_kind` fail-closed; bilinmeyen satır sessiz atlanmaz | `gates/cdp3c_preflight_objects.sh` |
| 3 | Guard üç-katman değildi | guard_selftest kendi DROP'unu guard'sız yapıyordu; preflight_selftest sentinel doğrulamadan drop; sentinel yalnız tablo-varlığı | `_ephemeral_guard.sh`: sentinel **MARKER değeri** (`CDP3C_CI_EPHEMERAL`) doğrulanır; guard_selftest başında `require_local_optin`; preflight_selftest başında `require_ephemeral` (opt-in+loopback+marker) İLK drop'tan önce; workflow selftest DB'yi önce baseline+sentinel ile hazırlar; negatifler: opt-in-yok/uzak-host/sentinel-yok/**marker-yanlış** | `gates/_ephemeral_guard.sh`, `gates/cdp3c_baseline.sql`, `gates/cdp3c_guard_selftest.sh`, `gates/cdp3c_preflight_selftest.sh`, workflow |
| 4 | "tüm public fn" matrisi prod ile uyuşmaz | canlı-katalog tüm public fn taraması → önceki-faz `trip_save` vb. yanlış reddederdi | **Manifest-scoped**: yalnız `cdp3c_objects.manifest` fonksiyonları denetlenir; NUMERIC `oid` ile doğrulama; aynı ada overload → fail-closed; 3 user RPC authenticated, service RPC service_role, helper/trigger kapalı; önceki-faz RPC'lere karışmaz; baseline'a `trip_save` (authenticated) fixture'ı → matrisin onu **reddetmediği** kanıtlanır | `gates/cdp3c_privilege_matrix.sh`, `gates/cdp3c_baseline.sql` |
| 5 | Her gate isimli GATE_FAILED değil | stack `ST="$(supabase status)"` set-e ile GATE_FAILED yazmadan çıkabilir | `set -uo pipefail`; `supabase init/start` `PIPESTATUS[0]` + `GATE_FAILED:stack_init`/`_start`; `status` `rc=$?` + `GATE_FAILED:stack_status`; `db_url` kontrolü; command-sub gate deseni tüm gate'lerde | `.github/workflows/cdp3c-gates.yml` |
| 6 | preflight_selftest komut hataları maskelenebilir | reset() adımları kontrolsüz; bazı mutasyon ON_ERROR_STOP'suz; obje-negatifleri yalnız metin arıyordu | reset'in HER adımı fail-closed (`reset_drop`/`reset_shim`/`reset_base`); tüm mutasyonlar `-v ON_ERROR_STOP=1`; obje-negatifleri hem `rc!=0` hem doğru hata kodu | `gates/cdp3c_preflight_selftest.sh` |
| 7 | Manifest/rapor v6/T33/eski sıra | başlık v7, gate listesi sentinel'i teardown'dan önce, T01–T33 | Manifest **v8** + gerçek sıra (gates→teardown→final scan→sentinel(if:success)→artifact) + T01–T39; rapor/test-matrisi v8; yerel-preflight EXPECTED-FAIL ayrımı korundu | `CDP3C_DELIVERY_MANIFEST.md`, `CDP3C_PACKAGE_REPORT.md`, `CDP3C_TEST_MATRIX.md` |
| 8 | Manifest üretici parser sınırı belirsiz | yalnız lowercase/tek-satır; overload ad-düzeyinde birleşir | gen **case-insensitive** ('create table [if not exists]'/'create type'/'create [or replace] function'); manifest_check ayrıca **CREATE-satır-sayısı == manifest-sayısı** (overload/format kaçağı fail-closed); `manifest_selftest.sh` (UPPERCASE tablo/type, multiline func, overload count tutarsızlığı); ad-authoritative + create-satır tutarlılığı açıkça belgelendi; 20/12/48 tek başına kanıt değil | `gates/gen_object_manifest.sh`, `gates/cdp3c_manifest_check.sh`, `gates/cdp3c_manifest_selftest.sh` |

## Yerel kanıt (BÖLÜM A/B/C — CI DEĞİL, production DEĞİL)
`gates/LOCAL_GATE_EVIDENCE.log`: manifest_check(20/12/48 + create-satır) · manifest_selftest · guard_selftest · secret_scan-selftest · baseline(+sentinel MARKER+trip_save) · preflight_objects · up + reapply · invariants · tests(**T01–T39**) · privilege_matrix(manifest-scoped, trip_save reddedilmedi) · concurrency(5 senaryo) · down_soft + downsoft_assert · preflight_selftest · secret_scan → **hepsi PASS** (exit code'lar log'da). Yerel preflight contract EXPECTED-FAIL; GitHub ephemeral CI + production preflight ÇALIŞTIRILMADI.

## Kill-switch
admin-delete-user kill-switch v3'e dokunulmadı.
