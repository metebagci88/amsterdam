# CDP-3C · Paket v1 → v2 değişiklik tablosu (15 düzeltme)

Aşağıda talep edilen 15 somut düzeltmenin her biri için **eski (v1) durum → yeni (v2) çözüm → etkilenen dosya(lar)** verilmiştir. Bu paket **production'a uygulanmadı**.

| # | Konu | Eski (v1) | Yeni (v2) | Etkilenen dosya |
|---|---|---|---|---|
| 1 | message_class vs email_class | `message_class` enum + kolon + immutability migration'daydı; sınıflandırma 3C'de | **Tamamen kaldırıldı**; e-posta sınıflandırma + `assertServiceContent` + send-time denetim **3D'ye ertelendi**. INV, `email_templates.message_class` ve `journeys` eklenmediğini doğrular | `CDP3C_up.sql`, `gates/cdp3c_invariants.sql`, `CDP3C_EDGE_ADMIN_SPEC.md`, `CDP3C_TEST_MATRIX.md`, `CDP3C_APPLY_ROLLBACK.md` |
| 2 | Consent grant doğrulama sertleştirme | grant, `text_version_id`'yi zayıf doğruluyordu; "unknown"=denied karıştırılıyordu; marketing capture kontrolü yoktu | purpose→doc_type **allowlist** (`consent_purpose_doc`) + aktif + **controller-bağlı** + locale doğrulama; `marketing_capture_enabled=false` iken grant → `marketing_capture_disabled`; withdraw serbest `text_version_id` almaz; default **absent** (denied değil), marketing fail-closed | `CDP3C_up.sql` (`_consent_set_internal`), `gates/cdp3c_tests.sql` (T02,T03,T04) |
| 3 | signup authenticated'a açık olmamalı | `consent_set_signup` authenticated'tan çağrılabiliyordu; source client'tan gelebiliyordu | `consent_set_signup` **kaldırıldı** → `consent_set_via_flow` yalnız **service_role**; `source` RPC içinde sabit; yalnız `consent_set_pref_center` authenticated'a açık | `CDP3C_up.sql` (grant matrisi), `gates/cdp3c_invariants.sql`, `gates/cdp3c_tests.sql` (T10), `CDP3C_EDGE_ADMIN_SPEC.md` |
| 4 | Geri çekme/suppression için gerçek kapalı RPC | Suppression yorumla tarif edilmişti; supersede kuralları belirsizdi | Gerçek RPC'ler: withdraw→`user_unsubscribe` suppression; grant **yalnız** user_unsubscribe supersede; `iys_red` yalnız `iys_supersede_red` (İYS sync kanıtı); hard_bounce/spam/admin_safety **otomatik kalkmaz**; HMAC + pepper_version; ham PII audit/idem/error'a girmez | `CDP3C_up.sql` (`admin_w_apply_suppression`,`iys_supersede_red`,`_consent_set_internal`), `gates/cdp3c_tests.sql` (T07,T09,T13,T14) |
| 5 | contact_suppression_current çok-neden | Tek satır neden → yeni neden eskisini eziyordu | PK `(channel,contact_hmac,scope,reason)` → **çok aktif neden** korunur; typed `scope` | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T08) |
| 6 | Gerçek immutability | Immutability yorum/kısmi | published consent_text gövde/hash/version/controller UPDATE reddi; controller published-immutability; attestation append-only **revoke-event**; revoke helper EXECUTE kapalı; typed controller alanları (mutual requirement CHECK) | `CDP3C_up.sql` (trigger'lar + CHECK), `gates/cdp3c_tests.sql` (T11,T12) |
| 7 | Tüm yazımlarda 0C + audit | Bazı yazımlar idem/audit'siz; enable `not p_enabled` ile before türetiyordu | actor+action+fingerprint + **post-lock idem recheck**; `request_id` zorunlu/trim/limit (`_require_request_id`); admin suppression audit; `set_marketing_enabled` request_id+idem+reason + before **`FOR UPDATE` ile gerçek satırdan** | `CDP3C_up.sql` (`_consent_idem_check`,`admin_w_set_marketing_enabled`), `gates/cdp3c_tests.sql` (T05,T06,T14,T16) |
| 8 | Readiness aktif metin↔aktif controller | Readiness zayıf/attestation tipi yok | `marketing_readiness_check` aktif marketing metninin **aktif controller'a bağlı** olduğunu kanıtlar; typed attestation koşulları (`readiness_domain`/`readiness_condition`); `_readiness_attested` helper | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T15,T16) |
| 9 | Anon consent kısıtı | Anon purpose serbest; TTL/expires belirsiz | anon purpose **CHECK** yalnız analytics/advertising; TTL endpoint aktifleşmeden karar (apply-blocker); `expires_at` server-türetilir; `anon_subject_create` TTL yoksa fail-closed | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T17) |
| 10 | Journey migration'dan çıkar | journey tanımı migration+rollback'teydi | journey/provider/outbox/send **migration'dan çıkarıldı** (3E); INV `journeys` yokluğunu doğrular | `CDP3C_up.sql`, `CDP3C_down_soft.sql`, `CDP3C_down_insecure.sql`, `gates/cdp3c_invariants.sql` |
| 11 | down_soft gevşetmemeli | v1 down bazı trigger/journey'i düşürüyordu | down_soft yalnız **dış RPC** düşürür; immutability/append-only trigger, retained HMAC helper, iş tabloları, RLS, pepper, kill-switch **korunur**; iş tablosu DROP yok | `CDP3C_down_soft.sql`, `gates/cdp3c-gates.yml` (soft-down invariant step) |
| 12 | Admin consent sorgusu allowlist | Yanıt fazla alan sızdırabiliyordu | `admin_q_member_consent` **açık allowlist** (purpose/state/epoch/updated_at + event action/source/at); fingerprint/idempotency/request_id/ham HMAC/evidence **DÖNMEZ** | `CDP3C_up.sql`, `CDP3C_EDGE_ADMIN_SPEC.md` |
| 13 | service_pref_defaults NULL çözümü | NULL sessizce false gibi davranıyordu | NULL → gerçek `config_pending` (RPC + `consent_get_my_state` yanıtı); **backfill yok** | `CDP3C_up.sql` (`service_pref_set`,`consent_get_my_state`), `gates/cdp3c_tests.sql` (T19) |
| 14 | "placeholder legal text" adımı kaldır | Apply adımı placeholder aktif metin yayımlıyordu | Adım kaldırıldı; onaylı gövde olmadan `is_active=true` **yok**; `marketing_capture_enabled` fail-closed false | `CDP3C_APPLY_ROLLBACK.md`, `CDP3C_up.sql` (default false) |
| 15 | PGlite tek başına kanıt değil | Doğrulama yalnız markdown/PGlite | **Ephemeral gerçek Supabase/Postgres CI** (CDP-3B deseni): migration/Vault/auth.uid/RLS FORCE/SECDEF/privilege matrisi/idempotency/up+soft-down invariant/zero-footprint teardown; **çalıştırılabilir SQL** dosyaları | `gates/cdp3c-gates.yml`, `gates/cdp3c_baseline.sql`, `gates/cdp3c_invariants.sql`, `gates/cdp3c_tests.sql` |

## v2'de bulunan + düzeltilen ek yapısal hata (build sırasında)
| Kök neden | Etki | Minimal çözüm | Dosya |
|---|---|---|---|
| Harness/up.sql önceki-faz nesnelerine (members, admin_users/roles, admin_write_log, `_admin_active`/`_admin_has_role`) dayanıyor; fresh ephemeral stack'te yoklar → CI migration/tests adımı patlardı | CI "runnable" değildi | **CI-only baseline test-double** (`gates/cdp3c_baseline.sql`) up.sql'den önce uygulanır; production'da gerçek nesneler zaten var, prod'a asla uygulanmaz | `gates/cdp3c_baseline.sql`, `gates/cdp3c-gates.yml` |
| `cdp3c_tests.sql`: `update ... limit 1` (Postgres UPDATE'te LIMIT yok) + `select status into ok` (boolean←text) | Test SQL çalışmazdı | LIMIT'li UPDATE kaldırıldı; hatalı satır kaldırıldı; assertion doğru hata nedenini kontrol eder | `gates/cdp3c_tests.sql` |
| Readiness'te plpgsql içinde nested SQL fonksiyon tanımı | Geçersiz PL/pgSQL | Standalone `_readiness_attested(readiness_domain,readiness_condition)` | `CDP3C_up.sql` |
