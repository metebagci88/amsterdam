# CDP-3C · Paket v8 → v9 değişiklik tablosu (2 blocker + 1 harness açığı)

v8 bağımsız incelemede **SQL çekirdeği temiz** bulundu → SQL iş mantığı/kapsamı **DEĞİŞTİRİLMEDİ**.
v9 YALNIZ 3 harness düzeltmesidir. Production'a uygulanmadı, CI koşulmadı, kill-switch v3 ve mevcut iki kullanıcı değişmedi, commit/push/Run/apply yapılmadı. Gömülü Postgres 16.2'de çalıştırılıp doğrulandı; ham kanıt `gates/LOCAL_GATE_EVIDENCE.log`.

| # | Konu | Eski (v8) | Yeni (v9) | Dosya |
|---|---|---|---|---|
| 1 | **preflight_objects HÂLÂ fail-open** | Kontrol `[ "$(qbool …)" = "t" ]` biçimindeydi; qbool'un `exit`'i **command-substitution alt-kabuğunda** kalıyordu → geçersiz DB URL'de sorgu hata verse bile ana akış `present=0` ile devam edip yanlış `CDP3C_PREFLIGHT_OBJECTS_PASS` yazabiliyordu | Sorgu exit code'u **ANA shell'de** yakalanır (`run_q` QRC + `bool_true`): rc≠0 → `GATE_FAILED:preflight_objects_query` (exit 1); değer yalnız tam `t`/`f`, aksi `GATE_FAILED:preflight_objects_bad_value`; manifest okunamaz / bilinmeyen kind fail-closed. **Kanıt:** `postgresql://127.0.0.1:1/nope` → exit=1, `GATE_FAILED:preflight_objects_query`, HİÇ PASS yok | `gates/cdp3c_preflight_objects.sh` |
| 2 | **Manifest üretici gerçek multiline CREATE'i kaçırır** | Satır-bazlı `grep -ioE 'create table … public\.'`; `CREATE TABLE`\n`public.x(…)` (ad sonraki satırda) YAKALANMAZDI. `manifest_selftest` "multiline" fixture'ı sahte (ad aynı satırda) | Üretici tüm dosyayı **yorum-strip (`--`, `/* */`) + whitespace-normalize** edip regex uygular → satır sınırı yok, gerçek multiline yakalanır; dollar-gövde silinmez (type'lar `do $$…$$` içinde). `--raw` (dup-inclusive) modu eklendi. `manifest_check` çapraz-sayımı artık `--raw` (multiline-aware) → overload/format kaçağı fail-closed. `manifest_selftest` **gerçek fixture**: CREATE anahtar sözcüğü + ad AYRI satır, uppercase, fazladan whitespace, yorum-içi sahte CREATE (sızmamalı), overload (raw=2/uniq=1). **Kanıt:** up.sql'e eklenen `CREATE TABLE`\n`public.hidden_multiline(id int)` → `table:hidden_multiline` CAUGHT | `gates/gen_object_manifest.sh`, `gates/cdp3c_manifest_check.sh`, `gates/cdp3c_manifest_selftest.sh` |
| 3 | **guard_selftest sentinel doğrulamadan mutasyon yapar** | Başta yalnız `require_local_optin` (opt-in+host); ardından sentinel tablosunu doğrudan DROP/CREATE/TRUNCATE ederdi — TAM 3-katman (sentinel MARKER) doğrulaması **ilk mutasyondan önce yoktu**. Workflow'da guard_selftest, sentinel'i yaratan baseline'dan **önce** koşuyordu | Script başında **TAM `require_ephemeral`** (opt-in + yerel host + sentinel MARKER `CDP3C_CI_EPHEMERAL`) → sentinel yoksa **hiçbir DROP yapmadan** exit 3. Tüm mutasyonlar `-v ON_ERROR_STOP=1` + rc denetimli (`mut`). Sonda doğru marker geri yüklenip **DOĞRULANIR** (aksi `GATE_FAILED:sentinel_restore`). 4 negatif (opt-in-yok / uzak-host / sentinel-yok / marker-yanlış) mutasyon-öncesi exit kanıtı. Workflow: **`baseline` artık `guard_selftest`'ten ÖNCE**. **Kanıt:** sentinel yokken exit=3 `ephemeral_sentinel_missing_or_wrong_marker`; baseline sonrası PASS | `gates/cdp3c_guard_selftest.sh`, `.github/workflows/cdp3c-gates.yml` |

## Yerel kanıt (BÖLÜM A/B/C — CI DEĞİL, production DEĞİL)
Taze DB reset + shim + tam CI-mirror sıra: manifest_check(20/12/48 + raw-declaration) · manifest_selftest(gerçek multiline/yorum/overload) · secret_scan-selftest · secret_scan(clean) · baseline · guard_selftest(baseline sonrası, 3-katman) · preflight_objects(PRE-APPLY PASS / POST-APPLY fail-closed) · up + reapply · invariants · privilege_matrix(trip_save reddedilmedi) · tests(**T01–T39**) · concurrency(5 senaryo) · down_soft + downsoft_assert · preflight_selftest(ayrı ephemeral DB) → **hepsi PASS** (exit code'lar `gates/LOCAL_GATE_EVIDENCE.log`). Yerel preflight contract **EXPECTED-FAIL** (shim'de gerçek Vault yok; CI'da PASS). GitHub ephemeral CI + production preflight **ÇALIŞTIRILMADI**.

## Değişmeyenler (reviewer "koru/GOOD" dedi)
Ayrı `DROP DATABASE`/`CREATE DATABASE` (`-c` + PIPESTATUS + isimli hata); selftest DB baseline+marker; manifest-scoped privilege matrix + `trip_save` fixture; isimli stack/status hataları; T01–T39; approve 0C intent-idem + legal_notice↔merge S5; SQL up.sql çekirdeği. **Hiçbiri değiştirilmedi.**

## Kill-switch
admin-delete-user kill-switch v3'e dokunulmadı.
