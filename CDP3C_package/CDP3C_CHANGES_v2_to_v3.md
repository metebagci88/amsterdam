# CDP-3C · Paket v2 → v3 değişiklik tablosu (18 statik-inceleme düzeltmesi)

Her madde: **eski (v2) → yeni (v3) çözüm → etkilenen dosya(lar)**. Paket **production'a uygulanmadı**, CI **koşulmadı**.

| # | Konu | Eski (v2) | Yeni (v3) çözüm | Etkilenen dosya |
|---|---|---|---|---|
| 1 | Workflow yanlış konumda | `CDP3C_package/gates/cdp3c-gates.yml` (GitHub keşfetmez) | Kanonik `**/.github/workflows/cdp3c-gates.yml`; `working-directory: CDP3C_package`; `pull_request.paths` iki yol; paket içi ikinci kopya **silindi** | `.github/workflows/cdp3c-gates.yml` (yeni), `gates/cdp3c-gates.yml` (silindi) |
| 2 | Workflow SHA doğrulamıyor | SHA gate yok, `latest` CLI, tek sentinel | İlk gate `sha256sum -c SHA256SUMS` (fail-closed); CLI **pinned** (1.226.4); her gate `GATE_FAILED:<ad>`; başarı yalnız `LOCAL_CDP3C_GATES_PASS`; loglar+teardown `if: always()` artifact; secret/token taraması; residue≠0 → kırmızı | `.github/workflows/cdp3c-gates.yml` |
| 3 | Immutability deactivate→mutate | `if OLD.is_published` — pasifleştirip mutate mümkündü | `is_published true→false YASAK` (BIU); published satır tüm kanonik alan + `published_address_form`/`valid_from`/`valid_to`/`published_at` ile kalıcı immutable; çalıştırılabilir test T11a–d | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 4 | Controller JSONB allowlist gerçek değil | `?&` yalnız varlık; NULL→UNKNOWN geçiyordu | `num_nonnulls=1` CHECK + `_validate_controller_fields` (IS NOT NULL, `jsonb_typeof='object'`, ekstra key=red, tip/uzunluk, mutual-excl, MERSİS yalnız legal 16-hane); negatif testler T15a–e | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 5 | Capture gate eksik | `_is_marketing_purpose` personalization/advertising'i dışlıyordu; analytics belirsiz | `_is_capture_gated` = email/sms/push + advertising + on_site_personalized + profiling; `analytics_storage` yalnız cookie akışı (`cookie_purpose_requires_cookie_banner`, pref_center'da `analytics_managed_by_cookie_flow`) | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` (T02,T17g) |
| 6 | Withdraw parametresi reddedilmiyordu | non-null `text_version_id` sessiz yok sayılıyordu | withdraw'da non-null → `withdraw_text_version_must_be_null` | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 7 | Source sözleşmesi SQL↔doküman uyuşmazlığı | Edge `unsubscribe_flow/admin_tool`; SQL `iys_sync`'i via_flow'da reddediyordu | Tek CANONICAL enum; via_flow `{signup,cookie_banner,unsubscribe,system}` + **matris** (unsubscribe=withdraw; cookie=analytics/advertising; signup≠marketing grant; system≠grant; import/iys_sync kapalı); testler T17a–g; spec §0 hizalı | `CDP3C_up.sql`, `CDP3C_EDGE_ADMIN_SPEC.md`, `gates/cdp3c_tests.sql` |
| 8 | Suppression lineage yanlış | current.source_event_id'ye consent event ID yazılıyordu; iys sahte UUID | Suppression event `RETURNING id` → current.source_event_id; supersede event → superseded_by; FK'ler eklendi; lineage testleri T07/T09/T16 | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 9 | `iys_supersede_red` kapsamı hatalı | channel/scope filtresiz; aktif yoksa sahte event | 64-hex + typed channel/scope parametre + WHERE; aktif yoksa `no_active_iys_red` (sahte event YOK); imza değişti | `CDP3C_up.sql`, `gates/cdp3c_invariants.sql`, `CDP3C_down_soft.sql`, `gates/cdp3c_tests.sql` (T16) |
| 10 | Hard-gate doğrudan UPDATE ile aşılıyordu | Yalnız RPC kontrolü; service-role tabloyu doğrudan güncelleyebiliyordu | `_marketing_config_guard` **DB trigger**: `marketing_enabled`/`marketing_capture_enabled` false→true readiness'e bağlı; doğrudan UPDATE bile reddedilir; capture readiness = legal_entity+metin+legal_signoff; testler T18a–c + downsoft #6 | `CDP3C_up.sql`, `gates/cdp3c_tests.sql`, `gates/cdp3c_downsoft_assert.sql` |
| 11 | Admin 0C tam değil | lock/recheck/fp eksik | `admin_w_apply_suppression`/`iys_supersede_red`/`set_marketing_enabled`/`set_capture` → advisory lock + kilit-sonrası recheck + fingerprint'e reason_text/evidence; aynı idem farklı gerekçe → conflict (T19) | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 12 | Anon akışı eksik | yalnız `anon_subject_create` | `anon_consent_set` (idempotency/expiry/purpose CHECK) + `anon_merge_into_user` (fail-closed, grant taşımaz, cleanup=merged) + `anon_ttl_days` 1..3650 CHECK; testler T20/T23 | `CDP3C_up.sql`, `CDP3C_EDGE_ADMIN_SPEC.md`, `gates/cdp3c_tests.sql` |
| 13 | Legal notice yazım yolu yok | UI yazacak ama RPC yok | `legal_notice_record` (aktif metin+doc+controller bağı, server ts/source, idem, **consent üretmez**); test T21 | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 14 | Config yönetimi doğrudan UPDATE | attestation/publish/default/ttl için RPC yok | `admin_w_add/revoke_readiness_attestation`, `admin_w_set_service_pref_default`, `admin_w_set_anon_ttl`, `admin_w_set_marketing_capture_enabled`, `admin_w_publish_controller_version`, `admin_w_publish_consent_text` (hepsi super_admin+reason+idem+audit+immutability; onaylı gövde <20 char aktifleştirilemez) | `CDP3C_up.sql`, `gates/cdp3c_invariants.sql`, `CDP3C_down_soft.sql` |
| 15 | `consent_purpose_doc` değiştirilebilir | mapping UPDATE/DELETE serbest | `_consent_purpose_doc_immutable` trigger → UPDATE/DELETE red; test T26 | `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 16 | Testler tüm iddiaları koşmuyor | T01–T19, bazıları yalnız markdown | T01–T29 çalıştırılabilir: deactivate→mutate, JSONB kapalı şema, iys lineage/scope, source×action×purpose, doğrudan config bypass, admin 0C conflict, anon set/merge/expiry, legal_notice consent üretmez, attestation revoke, PRE=POST, + workflow: ikinci apply + soft-down assert | `gates/cdp3c_tests.sql`, `gates/cdp3c_downsoft_assert.sql`, `.github/workflows/cdp3c-gates.yml`, `CDP3C_TEST_MATRIX.md` |
| 17 | Baseline ≠ prod parity | tek katman minimal double | İki katman: CI minimal baseline + **salt-okunur production compatibility preflight** (kolon/tip/RPC imza/admin_write_log/`_admin_active`-`_admin_has_role`/**members.email** kaynağı); ayrıca `_consent_set_internal` withdraw artık **fail-closed** (email çözülemezse `contact_email_unresolved_for_suppression`, sessiz başarı yok; T25) | `gates/cdp3c_preflight.sql` (yeni), `CDP3C_up.sql`, `gates/cdp3c_tests.sql` |
| 18 | Soft-down testi zayıf | yalnız varlık sayımı | `cdp3c_downsoft_assert.sql`: published immutability hâlâ enforce, append-only aktif, RLS deny-all+force, marketing enable/capture doğrudan açılamıyor, pepper+HMAC korunur, dış RPC gerçekten yok, iş tablosu/veri düşmedi | `gates/cdp3c_downsoft_assert.sql` (yeni), `.github/workflows/cdp3c-gates.yml` |

## Not — kill-switch
admin-delete-user kill-switch'e **dokunulmadı** (canlı v3 korunur). Bu paket onu içermez/değiştirmez.
